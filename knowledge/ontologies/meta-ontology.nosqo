#Type {
  isA #Type
  label "Type"
  description "A category of things."
  attribute ~label, ~description, ~isA, ~attribute
}

#Predicate {
  isA #Type
  label "Predicate"
  description "A relation between a subject and an object."
  attribute ~label, ~description, ~isA, ~targetType
}

#String {
  isA #Type
  label "String"
  description "A human-readable text value."
  attribute ~description
}

#Integer {
  isA #Type
  label "Integer"
  description "A whole number value."
  attribute ~description
}

#Decimal {
  isA #Type
  label "Decimal"
  description "A decimal number value."
  attribute ~description
}

#Date {
  isA #Type
  label "Date"
  description "A calendar date value."
  attribute ~description
}

#DateTime {
  isA #Type
  label "DateTime"
  description "A date and time value."
  attribute ~description
}

#Boolean {
  isA #Type
  label "Boolean"
  description "A true or false value."
  attribute ~description
}

#Symbol {
  isA #Type
  label "Symbol"
  description "An identifier-like literal value."
  attribute ~description
}

~isA {
  isA #Predicate
  label "is a"
  description "Declares that a subject is an instance of a type."
  targetType #Type
}

~label {
  isA #Predicate
  label "label"
  description "Provides a human-readable name."
  targetType #String
}

~description {
  isA #Predicate
  label "description"
  description "Provides a human-readable description."
  targetType #String
}

~attribute {
  isA #Predicate
  label "attribute"
  description "Declares that a type may use a predicate as an attribute."
  targetType #Predicate
}

~targetType {
  isA #Predicate
  label "target type"
  description "Declares an allowed target type of a predicate."
  targetType #Type
}
