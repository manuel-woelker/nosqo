# web

This directory is reserved for the browser-based `nosqo` UI.

The backend workspace is already set up in Rust.
The frontend can be added here once the API shape is stable enough to avoid churn.
