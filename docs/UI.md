# What is this document for?

This document is a pointer to the canonical UI documentation.
Use `ui/README.md` as the source of truth for the UI stack, design guidelines, routing expectations, state-management defaults, and frontend workflow.

# Where should UI guidance be maintained?

Maintain UI guidance in `ui/README.md`.
Do not duplicate the full guidance here unless there is a strong documentation reason to do so.
